# API defect report (for backend developer)

> Automation failed on a **business/API contract** check. This usually indicates a backend, SQL, or response-mapping issue — not a test-script typo.

## At a glance

| Field | Value |
|-------|-------|
| **API** | LF Analysis uniqueness (LF < 5% Last Three month) |
| **Test** | LF < 5% Last Three month — every page is checked so the same meter is not listed twice on the same DTR |
| **Endpoint** | GET https://api.mdm.mppkvvcl.bestinfra.app/analysis/commercial/lf?month=10&year=2025&type=LF+%3C+5%25+Last+Three+month&page=1&pageSize=500 |
| **Base URL** | https://api.mdm.mppkvvcl.bestinfra.app |
| **Response time** | 4732 ms |
| **HTTP status** | 200 |
| **Failed checks** | 1 / 1 |
| **First failure** | LF < 5% Last Three month collected uniqueness |
| **Run at** | 2026-09-15T14:07:25.508Z |

## AI / heuristic triage

| Field | Value |
|-------|-------|
| **Suggested title** | LF Analysis uniqueness (LF < 5% Last Three month) — LF < 5% Last Three month collected uniqueness |
| **Severity** | medium |
| **Classification** | contract_mismatch |
| **Likely owner** | backend |
| **Confidence** | 0.55 |
| **Source** | heuristic |

Duplicate LF Analysis msn=97790242 dtr=rj6612 with same value=3.05

### Recommended next steps

1. Expected behavior: meterLookupId unique. Same MSN with different LF is allowed. Same MSN with the same LF is a duplicate.
2. Diff actual JSON fields against mapper/validator expectations.
3. Confirm fixture month/year/IDs still exist in this environment.
4. Share this defect report with the API owner for the module.

**Tags:** contract, COMMERICIAL-ANALYSIS, LF < 5% Last Three month collected uniqueness


## Failure detail

```
Duplicate LF Analysis msn=97790242 dtr=rj6612 with same value=3.05

[2mexpect([22m[31mreceived[39m[2m).[22mtoBeFalsy[2m()[22m

Received: [31mtrue[39m
```

## Expected behavior (from backend / contract)

meterLookupId unique. Same MSN with different LF is allowed. Same MSN with the same LF is a duplicate.


## Request sent by automation

```json
{
  "month": 10,
  "year": 2025,
  "type": "LF < 5% Last Three month",
  "page": 1,
  "pageSize": 500
}
```

## API response (excerpt)

```json
{
  "total": 14993,
  "collected": 14993
}
```

## All failed validations

1. **LF < 5% Last Three month collected uniqueness** — Duplicate LF Analysis msn=97790242 dtr=rj6612 with same value=3.05

[2mexpect([22m[31mreceived[39m[2m).[22mtoBeFalsy[2m()[22m

Received: [31mtrue[39m

## Passed checks before failure

_None_

## Suggested steps for developer

1. Reproduce with the same query params (Postman/curl) against `https://api.mdm.mppkvvcl.bestinfra.apphttps://api.mdm.mppkvvcl.bestinfra.app/analysis/commercial/lf?month=10&year=2025&type=LF+%3C+5%25+Last+Three+month&page=1&pageSize=500`.
2. Compare response with the **Expected behavior** / **AI triage** sections above.
3. Trace the repository/service method for this report and confirm SQL filters + DTO field names.
4. Fix backend logic or API serialization; re-run: `npx playwright test --grep "LF Analysis uniqueness (LF < 5% Last Three month)"`.

---
*Generated by Indoore API automation — share this file in Jira/Azure DevOps/GitHub issue.*
